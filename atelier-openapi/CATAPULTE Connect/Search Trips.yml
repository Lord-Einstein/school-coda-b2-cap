info:
  name: Search Trips
  type: http
  seq: 3

http:
  method: POST
  url: POST {{baseUrl}}/trips/search
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
