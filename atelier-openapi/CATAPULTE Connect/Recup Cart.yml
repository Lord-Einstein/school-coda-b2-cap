info:
  name: Recup Cart
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/carts"
  auth:
    type: bearer
    token: string

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
