info:
  name: Check Tickets
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/tickets"
  auth:
    type: bearer
    token: string

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
