info:
  name: Create Cart
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/carts"
  auth:
    type: bearer
    token: string

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
